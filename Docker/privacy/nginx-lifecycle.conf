# REQUIRED companion to the Go privacy-process runner's Linux lifecycle checks.
# Include this file in nginx's MAIN context, outside events/http/server/location.
# File presence does not prove inclusion; verify the effective config and held-request fixture.
# Native graceful shutdown closes remaining held connections after this deadline.
worker_shutdown_timeout 2s;
